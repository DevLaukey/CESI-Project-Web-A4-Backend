{
  "name": "generated_backend",
  "version": "1.0.0",
  "description": "backend config",
  "main": "src/server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required for this microservice'",
    "db:test": "npx sequelize-cli db:create --env test && npx sequelize-cli db:migrate --env test",
    "db:init": "npx sequelize-cli db:create",
    "db:migrate": "npx sequelize-cli db:migrate",
    "db:seed": "npx sequelize-cli db:seed:all"
  },
  "keywords": [
    "express",
    "backend",
    "generated_backend"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "cookie-parser": "^1.4.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-custom-error": "^1.8.4",
    "express-rate-limit": "^7.5.0",
    "has-keys": "^0.1.0",
    "helmet": "^3.21.2",
    "http-status": "^1.3.2",
    "mandatoryenv": "^1.1.1",
    "module-alias": "^2.2.2",
    "morgan": "^1.9.1",
    "mysqlm": "^2.0.3",
    "rotating-file-stream": "^1.4.6",
    "socket.io": "^4.8.1",
    "uuid": "^11.1.0"
  }
}
